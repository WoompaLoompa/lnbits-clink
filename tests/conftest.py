# No sys.path tricks needed: tests are invoked via tests/run_tests.sh, which
# mounts this repo under a temp dir named `clink` so the package imports
# exactly as LNbits loads it (see Makefile test target).
